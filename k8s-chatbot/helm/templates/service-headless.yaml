apiVersion: v1
kind: Service
metadata:
  name: k8s-chatbot-headless
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: k8s-chatbot
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: k8s-chatbot
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.port }}
      name: http
