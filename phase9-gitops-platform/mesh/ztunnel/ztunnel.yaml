apiVersion: sailoperator.io/v1alpha1
kind: ZTunnel
metadata:
  name: default
spec:
  namespace: ztunnel
  # Không set spec.profile: CRD v1 (conversion) không có field đó
  # → Argo SSA: ".spec.profile: field not declared in schema"
