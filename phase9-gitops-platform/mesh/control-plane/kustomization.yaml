apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - istio.yaml
  # IstioCNI: mesh/bootstrap/ — apply tay (Argo không sync spec.profile)
  - telemetry.yaml
  - istiod-servicemonitor.yaml
