# APPLY TAY — KHÔNG qua Argo (schema v1 không khai báo spec.profile).
apiVersion: sailoperator.io/v1alpha1
kind: ZTunnel
metadata:
  name: default
spec:
  namespace: ztunnel
  profile: ambient
  values:
    seLinuxOptions:
      type: spc_t
