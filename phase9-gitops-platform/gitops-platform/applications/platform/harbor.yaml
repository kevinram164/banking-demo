# Harbor — dev-ocp: harbor-banking.apps.ocp01.npd.co
# Xem harbor/README.md: TLS, admin password, robot accounts
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-harbor
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: phase9-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: banking-platform
  source:
    repoURL: https://helm.goharbor.io
    chart: harbor
    targetRevision: 1.15.0
    helm:
      releaseName: harbor
      values: |
        expose:
          type: clusterIP
          tls:
            enabled: false
        externalURL: https://harbor-banking.apps.ocp01.npd.co
        harborAdminPassword: "ChangeMe-Harbor-Admin"
        internalTLS:
          enabled: false
        # Lab k3d: tắt Trivy giảm RAM + thời gian khởi động
        trivy:
          enabled: false
        database:
          type: internal
          internal:
            password: "ChangeMe-Harbor-DB"
            shmSizeLimit: 256Mi
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 512Mi
        redis:
          type: internal
          internal:
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 256Mi
        core:
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
          startupProbe:
            enabled: true
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 36
          livenessProbe:
            initialDelaySeconds: 60
        jobservice:
          maxJobWorkers: 2
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 512Mi
        registry:
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
        portal:
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
        nginx:
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
        persistence:
          enabled: true
          persistentVolumeClaim:
            registry:
              storageClass: nfs-csi
              size: 10Gi
            jobservice:
              jobLog:
                storageClass: nfs-csi
                size: 1Gi
            database:
              storageClass: nfs-csi
              size: 2Gi
            redis:
              storageClass: nfs-csi
              size: 1Gi
  destination:
    server: https://kubernetes.default.svc
    namespace: platform
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
