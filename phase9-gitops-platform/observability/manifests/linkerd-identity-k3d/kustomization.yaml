apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: linkerd

secretGenerator:
  - name: linkerd-identity-issuer
    type: Opaque
    files:
      - ca.crt=certs/ca.crt
      - tls.crt=certs/issuer.crt
      - tls.key=certs/issuer.key
    options:
      disableNameSuffixHash: true
      labels:
        linkerd.io/control-plane-component: identity

configMapGenerator:
  - name: linkerd-identity-trust-roots
    files:
      - ca.crt=certs/ca.crt
    options:
      disableNameSuffixHash: true
      labels:
        linkerd.io/control-plane-component: identity
