# Banking UI — npd-banking.co (custom domain, cần DNS/hosts trỏ OpenShift Router)
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: npd-banking
  namespace: npd-banking
  labels:
    app.kubernetes.io/name: banking-demo
    app.kubernetes.io/component: frontend
    banking-demo/environment: dev-ocp
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  host: npd-banking.co
  path: /
  to:
    kind: Service
    name: frontend
    weight: 100
  port:
    # Tên port trên Service (port 80 → pod 8080). Số 80 khiến Router thấy Endpoints: <none>
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
