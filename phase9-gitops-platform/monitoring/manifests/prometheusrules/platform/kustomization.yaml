apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Platform Prometheus only. Needs cluster-admin on openshift-monitoring.
# UWM app rules stay in the parent kustomization.yaml.
resources:
  - nodes.yaml
  - disk.yaml
  - network.yaml
  - controlplane.yaml
