apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - banking.yaml
  - shop.yaml
  - infra.yaml
  # platform/ (nodes+disk+network+controlplane) + banking-kube-platform.yaml
  # → openshift-monitoring — apply riêng, cần cluster-admin:
  #   oc apply -k .../prometheusrules/platform
  #   oc apply -f .../prometheusrules/banking-kube-platform.yaml
